# 📌 TASK: TASK-0004 - Tích hợp Dashboard UI Backtest & Paper Trading, Nối dây Tín hiệu Web sang Discord DM, và Cách ly Môi trường Kiểm thử

**Mã Task:** `TASK-0004`  
**Ngày tạo:** 2026-09-24  
**Người khởi xướng:** Client (Tùng - Yêu cầu từ `Client.md` v3.1)  
**Product Owner:** PO Agent  
**Trạng thái:** `DONE`  
**Mức độ ưu tiên:** `P1 - Critical`  

---

## 1. Bối cảnh & Mục tiêu Nghiệp vụ (Business Context)

### 1.1. Vấn đề Hiện tại (Pain Points từ Client.md v3.1)
1. **Thiếu giao diện trực quan cho Backtest & Paper Trading:**  
   Backend lõi định lượng đã hoàn tất trong `TASK-0003` nhưng người dùng chưa có giao diện trên Streamlit (cả Local và PROD) để bấm nút chạy thử, tùy chọn mã cổ phiếu, xem biểu đồ đường vốn NAV so với VN-Index và bảng số liệu bóc tách 4 cột (FULL, UPTREND, DOWNTREND, SIDEWAYS).
2. **Đứt gãy kênh cảnh báo từ Web sang Discord DM:**  
   Khi người dùng bấm phân tích cổ phiếu trên Tab 5 (AI Strategy Analysis) và hệ thống cho tín hiệu MUA rõ ràng (điển hình như trường hợp **TCB ngày 23/09, giá vào 33k, Target 42.9k, ID 37** lưu trong Supabase), tín hiệu chỉ hiển thị trên màn hình Web mà **hoàn toàn KHÔNG gửi tin nhắn cảnh báo vào Discord DM** của Client.
3. **Ô nhiễm dữ liệu kiểm thử (Test Pollution vào Discord DM):**  
   Khi chạy bộ unit test tự động của hệ thống, các hàm gửi alert chưa được cô lập (mock) triệt để, dẫn đến việc bot gửi các mã test lạ (`HOT1`, `HOT2`, `TRAP1`) và kịch bản giả lập (`FPT giá 80k lỗ -20%`) vào Discord DM thật của Client, gây hoang mang và hiểu nhầm về trạng thái danh mục thật.
4. **Spam thông báo thanh lọc Watchlist:**  
   Tính năng dọn dẹp cổ phiếu quá mua/bẫy giá bị kích hoạt nhiều lần hoặc gửi dồn dập trong phiên; Client yêu cầu: **Tính năng thanh lọc cổ phiếu khỏi Watchlist chỉ được thực hiện trước phiên ATO (08:45) và chỉ làm ĐÚNG 1 LẦN DUY NHẤT trong ngày**.

### 1.2. Mục tiêu Đạt được (Objectives)
- **Tích hợp Dashboard UI Backtest & Paper Trading trên Streamlit:** Cung cấp bộ điều khiển chọn mã, xem bảng 4 cột Regime và theo dõi lệnh ảo Paper Trading ngay trên giao diện Web.
- **Nối dây tự động Web AI Strategy Analysis $\rightarrow$ Discord DM:** Mỗi khi phân tích mã trên Tab 5 mà hội đồng định lượng ra khuyến nghị **`MUA`**, hệ thống tự động gửi 1 Rich Embed tóm tắt ngay vào Discord DM của Client.
- **Cách ly 100% môi trường kiểm thử (Strict Test Isolation):** Đảm bảo mọi file unit test đều mock các hàm gửi Discord, cấm tuyệt đối việc rò rỉ dữ liệu test vào kênh thông báo thật.
- **Cố định lịch thanh lọc Watchlist trước ATO:** Chỉ quét dọn Watchlist vào lúc 08:45 sáng (1 lần/ngày), nghiêm cấm quét lặp lại trong phiên.

---

## 2. Câu chuyện Người dùng (User Stories)

- **US-1 (Nhà đầu tư cá nhân - Trải nghiệm UI Backtest trực quan):**  
  *Là một* nhà đầu tư cá nhân,  
  *Tôi muốn* có giao diện trực quan trên Web để chọn mã cổ phiếu, bấm nút chạy Backtest và xem ngay đồ thị đường vốn so với VN-Index cùng bảng chỉ số 4 Regime,  
  *Để* tôi dễ dàng đánh giá chiến lược mà không cần phải chạy code bằng dòng lệnh.

- **US-2 (Nhà đầu tư cá nhân - Nhận tín hiệu MUA tức thì qua Discord DM):**  
  *Là một* nhà đầu tư theo dõi thị trường qua điện thoại,  
  *Tôi muốn* mỗi khi tôi hoặc hệ thống phân tích ra tín hiệu MUA trên Tab AI (như case TCB), bot phải tự động gửi bản tóm tắt vào Discord DM cho tôi,  
  *Để* tôi không bị lỡ cơ hội đầu tư ngay cả khi không nhìn màn hình máy tính.

- **US-3 (Client - Kênh Discord sạch sẽ & Không bị spam):**  
  *Là một* người sử dụng bot,  
  *Tôi muốn* kênh Discord DM của mình chỉ nhận các thông báo thật từ thị trường, tuyệt đối không bị dính mã test ảo (`HOT1`, `TRAP1`, `FPT lỗ -20%`) và thông báo dọn dẹp Watchlist chỉ gửi đúng 1 lần trước phiên ATO (08:45),  
  *Để* tôi không bị làm phiền và luôn tin tưởng 100% vào tính chính xác của các cảnh báo.

---

## 3. Tiêu chí Chấp thuận (Acceptance Criteria - AC)

### Nhóm AC-1: Giao diện UI Backtest & Paper Trading trên Streamlit
- **AC-1.1:** *Given* người dùng truy cập Tab 6 (Alpha Tracker) hoặc khu vực Backtest trên Streamlit, *When* chọn một mã cổ phiếu (ví dụ HPG, FPT) và bấm nút "🚀 Chạy Backtest Theo Regime", *Then* hệ thống phải gọi `RegimeBacktestEngine` và hiển thị bảng số liệu định lượng 4 cột: `FULL`, `UPTREND`, `DOWNTREND`, `SIDEWAYS` (CAGR, Max Drawdown, Sharpe, Sortino, Win Rate, Expectancy).
- **AC-1.2:** *Given* kết quả Backtest hoàn tất, *When* hiển thị đồ thị, *Then* hệ thống phải render biểu đồ đường vốn (Equity Curve) trực quan hóa mức tăng trưởng tài sản theo thời gian.
- **AC-1.3:** *Given* khu vực Paper Trading trên giao diện, *When* tải trang, *Then* bảng Paper Trading phải hiển thị danh mục lệnh ảo, tỷ lệ khớp lệnh (%) và mức trượt giá trung bình (bps).

### Nhóm AC-2: Nối dây Tín hiệu Mua từ Web sang Discord DM (Web-to-Discord Hook)
- **AC-2.1:** *Given* người dùng bấm phân tích một mã cổ phiếu trong Tab 5 (hàm `analyze_stock_with_smart_committee` trong `ai_analyst.py`), *When* hành động định lượng là `MUA` (hoặc `VALUE BUY`, `ACCUMULATE`), *Then* hệ thống phải tự động gọi `send_trade_signal_alert()` gửi thông báo Rich Embed vào Discord DM của Client.
- **AC-2.2:** *Given* thông báo Mua từ Web gửi vào Discord DM, *When* Client xem tin nhắn, *Then* nội dung phải có đầy đủ: Mã CP, Thị giá hiện tại, Giá Target, Ngưỡng Stop-Loss, Điểm F-Score, Biên an toàn MoS (%) và tóm tắt lý do trọng yếu.

### Nhóm AC-3: Cách ly Triệt để Môi trường Kiểm thử (Strict Test Isolation)
- **AC-3.1:** *Given* toàn bộ bộ test suite trong `tests/` (bao gồm `test_corporate_actions_and_ai.py`, `test_task_0001_watchlist_and_noon.py`, `test_discord_alerts_no_truncation.py`...), *When* chạy `pytest`, *Then* 100% các hàm gửi alert (`send_trade_signal_alert`, `send_watchlist_pruned_alert`, `send_discord_dm`) phải được mock hoàn toàn.
- **AC-3.2:** *Given* quá trình chạy test tự động, *Then* số lượng request mạng gửi tới Discord Webhook/Bot API thật bắt buộc phải bằng **0**.

### Nhóm AC-4: Chuẩn hóa Lịch Thanh lọc Watchlist (Trước ATO - 1 lần/ngày)
- **AC-4.1:** *Given* tiến trình `trading_bot.py`, *When* kiểm tra điều kiện dọn dẹp Watchlist (`prune_unsuitable_watchlist`), *Then* hệ thống chỉ được phép thực thi vào khung giờ **08:45 sáng (trước phiên ATO)** và chỉ chạy đúng **1 lần duy nhất trong ngày giao dịch**.
- **AC-4.2:** Khi dọn dẹp các mã thật, báo cáo gửi Discord DM phải ghi rõ tên mã niêm yết thật, phân biệt rõ mã do Client thêm thủ công vs mã auto, kèm chỉ số định lượng cụ thể.

---

## 4. Thẩm định & Ký duyệt Tài chính (Finance Lead Sign-off)

*Chuyên viên Tài chính & Quản trị Rủi ro (Finance Lead) kiểm định theo `ROLES/finance.md`:*

- [x] **Bảo vệ Trải nghiệm Khách hàng & Kênh Cảnh báo:** Kịp thời chuyển tải tín hiệu MUA định lượng từ Web về Discord DM của Client để không bỏ lỡ cơ hội.
- [x] **Toàn vẹn Dữ liệu Kênh Alert:** Ngăn chặn triệt để dữ liệu rác từ bài test làm sai lệch nhận định thị trường của nhà đầu tư.
- [x] **Kiểm soát Tần suất Thông báo:** Chặn spam thanh lọc danh mục, đảm bảo thông báo chỉ phát đúng thời điểm có giá trị hành động cao nhất (trước ATO).
- [x] **Chi phí Vận hành:** Giao diện Streamlit tái sử dụng engine Python sẵn có, 0 VNĐ chi phí phát sinh.

**Kết luận Thẩm định:** `[ APPROVED ]`  
*Ghi chú của Finance Lead:* Đạt chuẩn quản trị rủi ro thông tin và bảo vệ kênh cảnh báo Client. Cho phép Senior Dev triển khai.  
*Ký tên:* Finance Lead Agent — Ngày: 2026-09-24

---

## 5. Phương án Kỹ thuật & Phạm vi Files (Senior Dev Design)

### 5.1. Thiết kế Giải pháp (Lazy Senior Dev)
1. `tabs/tab_alpha_tracker.py`: Bổ sung module giao diện Backtest trực quan và bảng theo dõi Paper Trading, gọi trực tiếp `RegimeBacktestEngine` và `PaperTradingManager`.
2. `ai_analyst.py`: Bổ sung lời gọi `send_trade_signal_alert()` sau khi `save_quant_signal()` thành công nếu `action_state` là `MUA` hoặc chứa `"BUY"`.
3. `trading_bot.py`: Bổ sung chốt chặn `pruned_today_flag` để đảm bảo `prune_unsuitable_watchlist()` chỉ chạy đúng 1 lần vào phiên 08:45 sáng.
4. `tests/`: Rà soát và bọc mock cho `test_corporate_actions_and_ai.py` và `test_task_0001_watchlist_and_noon.py`.

### 5.2. Danh sách Files Tác động
- `[MODIFY]` [tabs/tab_alpha_tracker.py](file:///c:/Users/Window/Desktop/Stock%20-%20learning/tabs/tab_alpha_tracker.py)
- `[MODIFY]` [ai_analyst.py](file:///c:/Users/Window/Desktop/Stock%20-%20learning/ai_analyst.py)
- `[MODIFY]` [trading_bot.py](file:///c:/Users/Window/Desktop/Stock%20-%20learning/trading_bot.py)
- `[MODIFY]` [tests/test_corporate_actions_and_ai.py](file:///c:/Users/Window/Desktop/Stock%20-%20learning/tests/test_corporate_actions_and_ai.py)
- `[MODIFY]` [tests/test_task_0001_watchlist_and_noon.py](file:///c:/Users/Window/Desktop/Stock%20-%20learning/tests/test_task_0001_watchlist_and_noon.py)
- `[NEW]` [tests/test_task_0004_ui_and_web_hook.py](file:///c:/Users/Window/Desktop/Stock%20-%20learning/tests/test_task_0004_ui_and_web_hook.py)

---

## 6. Kế hoạch Kiểm thử & Độ phủ (QA Lead Verification)

- **File Test:** `tests/test_task_0004_ui_and_web_hook.py`
- **Kịch bản Happy Path:**
  - Phân tích Web cho kết quả BUY $\rightarrow$ Tự động bắn thông báo Discord DM [PASSED].
  - Thanh lọc Watchlist chỉ chạy đúng 1 lần vào khung 08:45 [PASSED].
  - 100% unit tests chạy offline không gọi network Discord thật [PASSED].
- **Mục tiêu Coverage:** Code Coverage trên mã mới đạt $\ge 85\%$ (147/147 test cases passed).
- **Ký duyệt:** `[ PASSED ]` — Trưởng nhóm QA: QA Lead Agent — Ngày: 2026-09-24

---

## 7. Kiểm duyệt Mã nguồn (Reviewer Audit)

- **Linter Output:** `ruff check . --output-format=github` -> Exit code 0 `[ PASSED ]`
- **SonarCloud Audit:** S8572 (Logging exceptions properly) `[ PASSED ]`, S3776 (Cognitive complexity < 15) `[ PASSED ]`, S1192 (Constants for string literals) `[ PASSED ]`, F401 (Zero unused imports) `[ PASSED ]`
- **Mật độ Trùng lặp:** New Duplicated Lines $\le 3.0\%$ `[ PASSED ]`
- **Đóng dấu duyệt:** `[ LGTM ]` — Reviewer: Reviewer Agent — Ngày: 2026-09-24

---

## 8. Bàn giao & Nghiệm thu từ Client (Client Acceptance)

- **Báo cáo Nghiệm thu:** Walkthrough artifact hoàn thành Phase 1-6 của TASK-0004.
- **Trạng thái cuối cùng:** `[ ACCEPTED ]`

